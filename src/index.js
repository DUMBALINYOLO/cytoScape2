import React from "react";
import ReactDOM from "react-dom";
import PolicyTool from "./policytools/policytools";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <PolicyTool />
  </React.StrictMode>,
  rootElement
);
