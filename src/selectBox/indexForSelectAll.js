import React from "react";
import ReactDOM from "react-dom";

import { Box } from "./selectAllButton";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <Box />
  </React.StrictMode>,
  rootElement
);
